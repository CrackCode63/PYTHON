# We are going to learn OS module
# What is OS module?
# The os module in Python provides a way of using operating system dependent functionality.
# This module is platform independent and hence can be used on any platform.

# OS module in python
import os
print(os.name)  # prints the name of the operating system

# Output: nt (for windows) or posix (for linux/mac)
