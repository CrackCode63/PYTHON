# We are going to learn tuples
# What is tuples?
# Tuples are like lists but they are immutable. They are like a read-only list.
# Tuples are like lists but they are immutable
# Tuples are used to store multiple items in a single variable
# Tuples are written with round brackets
# Tuples are used to store multiple items in a single variable
# Tuples items are seperated with the commas and enclosed within round brackets.


# Examples

tup = (1,2,3,4)
print(tup)
print(type(tup))

# Creating a tuple with one item
tup1 = (1,) #It consist commas after first element. It tells the python compilter that it is a tuple not a integer number.
print(tup1)